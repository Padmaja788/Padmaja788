#include <stdio.h>
void main() 
{
   int num,i;
   printf("enter a number:");
   scanf("%d",&num);
   printf("factors of the given number are:");
   for(i=1;i<=num;i++)
   {
       if(num%i==0)
       {
           printf("%d\t",i);
          
       }
   }
}
  
