#include <stdio.h>
#include<string.h>
void main()
{
    char pwd[10];
    char ch;
    int i=0;
    printf(" enter the password:");
    while((ch=getch())!=13)
    {
        pwd[i]=ch;
        printf("*");
        i+=1;
    }
    printf("The password is %d",pwd);
}
